declare const validator: any;
/**
 * Forces url to start with http:// if it doesn't
 *
 * @param {string} url - url to be updated
 * @return {string} url that starts with http
 *
 */
declare const coerceUrl: (url: any) => any;
